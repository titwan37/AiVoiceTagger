export const environment = {
    production: true,
    useMockData: true,       // Runs self-contained realistic stream generator
    apiUrl: '',
    wsUrl: '',               // WebSocket endpoint for real telemetry
    mockIntervalMs: 2000     // 2-second telemetry heartbeat
};
